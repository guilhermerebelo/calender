export type TravelEvent = {
  id: string;
  date: string;
  time: string;
  title: string;
  comments: string;
  createdAt: string;
  updatedAt: string;
};

export type EventDraft = {
  title: string;
  date: string;
  time: string;
  comments: string;
};
